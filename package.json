{
  "name": "us-funding-dashboard",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute funding-dashboard-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute funding-dashboard-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
